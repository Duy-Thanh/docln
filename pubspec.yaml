name: docln
description: "Light Novel Reader written in Flutter (for Android and iOS)"
publish_to: 'none'
version: 2025.10.12

environment:
  sdk: ^3.9.2

dependencies:
  flutter_localizations:
    sdk: flutter
  flutter:
    sdk: flutter
  dynamic_color: ^1.8.1
  palette_generator: ^0.3.3+7
  image_picker: ^1.2.0
  sqflite: ^2.4.2
  http: ^1.5.0
  html: ^0.15.6
  shared_preferences: ^2.5.3
  fluttertoast: ^9.0.0
  webview_flutter: ^4.13.0
  provider: ^6.1.5+1
  flutter_local_notifications: ^19.4.2
  permission_handler: ^12.0.1
  app_settings: 5.1.1 # fix
  flutter_launcher_icons: ^0.14.4
  workmanager: ^0.9.0+3 # Background task scheduling
  share_plus: ^12.0.0
  url_launcher: ^6.3.2
  android_intent_plus: ^6.0.0
  package_info_plus: ^9.0.0
  intl: ^0.20.2
  translator: ^1.0.4+1
  hive_flutter: ^1.1.0
  # Fast, lightweight key-value storage
  hive: ^2.2.3
  # Cache network requests
  dio_cache_interceptor: ^4.0.5
  # Persistent cache storage
  cached_network_image: ^3.4.1
  # Error tracking
  # sentry_flutter: ^9.6.0
  # Efficient image loading and caching
  flutter_cache_manager: ^3.4.1
  # Image compression
  flutter_image_compress: ^2.4.0
  # Lazy loading images
  lazy_load_scrollview: ^1.3.0
  dio: ^5.9.0
  open_file: ^3.5.10
  shimmer: ^3.0.0
  # firebase_analytics: ^11.4.5
  # firebase_crashlytics: ^4.3.5
  timeago: ^3.7.1
  path_provider: ^2.1.5
  wireguard_flutter: ^0.1.3
  file_picker: ^10.3.3 # For importing/exporting preferences
  # Encryption libraries
  encrypt: ^5.0.3
  crypto: ^3.0.6
  flutter_secure_storage: ^9.2.4
  # SQLite encryption
  sqlcipher_flutter_libs: ^0.6.8
  # State management with BLoC pattern
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  json_annotation: ^4.9.0
  firebase_messaging: ^16.0.2
  firebase_core: ^4.1.1
  app_links: ^6.4.1  # Deep linking support for custom URL schemes

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  hive_generator: ^2.0.1
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
